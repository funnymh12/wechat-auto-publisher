# wechat-auto-publisher

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

**公众号文章自动发布工具** — 写好文章，一行命令完成 Unsplash 配图 + 七牛云上传 + 推送到微信公众号编辑器。

## 功能

- 🖼️ 按关键词从 **Unsplash** 自动获取高质量封面图
- ☁️ 封面图自动上传 **七牛云**，生成永久 URL
- 📋 将本地 HTML 文章内容复制到剪贴板，粘贴进微信编辑器（保留富文本样式）
- 📝 自动填写标题、粘贴正文、上传封面
- 🔐 扫码登录一次，后续自动复用登录状态（`auth.json`）
- 🖥️ 浏览器保持打开，由你检查内容后手动点发布

## 安装

```bash
# 1. 克隆项目
git clone https://github.com/your/wechat-auto-publisher.git
cd wechat-auto-publisher

# 2. 安装依赖
npm install

# 3. 安装 Playwright 浏览器
npx playwright install chromium

# 4. 配置
cp config.example.json config.json
# 用编辑器打开 config.json，填入 Unsplash API Key 和七牛云信息
```

## 使用

**第一步**：把你的文章内容写进 `article_preview.html` 的 `#article` 区域。

> 参考 `article_template.html`，里面有每种组件的示例（Hero 横幅、功能列表、代码块、CTA 等）。  
> ⚠️ 每个元素必须写 `style="..."` 内联样式，微信会剥离 CSS class。

**第二步**：在 `publish.js` 顶部填写文章标题：

```javascript
const ARTICLE_TITLE = '文章标题';
```

**第三步**：运行：

```bash
node publish.js
```

第一次运行需要扫码登录微信公众平台，之后自动复用。

## 配置说明

| 字段 | 说明 |
|------|------|
| `articleTitle` | 文章标题（也可直接在 `publish.js` 中改） |
| `unsplashQuery` | Unsplash 搜图关键词，如 `"productivity workspace minimal"` |
| `unsplashAccessKey` | Unsplash 免费 API Key，从 [unsplash.com/developers](https://unsplash.com/developers) 获取 |
| `qiniu.accessKey` | 七牛云 Access Key |
| `qiniu.secretKey` | 七牛云 Secret Key |
| `qiniu.bucket` | 存储空间名称 |
| `qiniu.domain` | 你的 CDN 加速域名 |
| `qiniu.region` | 区域：`z0`(华东) `z1`(华北) `z2`(华南) `na0`(北美) `as0`(东南亚) |

## 项目结构

```
wechat-auto-publisher/
├── publish.js              # 主脚本
├── article_template.html   # 文章 HTML 模板（含组件示例）
├── article_preview.html    # 当次发布的文章（.gitignore 忽略）
├── config.example.json     # 配置模板（含说明）
├── config.json             # 你的实际配置（不会被提交）
├── auth.json               # 微信登录状态缓存（不会被提交）
├── package.json
└── LICENSE
```

## 常见问题

**Q：文章粘贴进去后排版乱了？**  
A：检查 `article_preview.html` 中的元素是否都有 `style=""` 内联样式。微信编辑器会整体保留行内样式，但会删除 CSS class 引用的样式。

**Q：封面图上传失败？**  
A：脚本会输出七牛云封面图永久链接，可以手动在编辑器中上传同一张图。

**Q：登录过期了？**  
A：删除 `auth.json` 或等待脚本自动检测过期并提示重新扫码。

**Q：Unsplash 图片不合适？**  
A：修改 `config.json` 中的 `unsplashQuery`，再运行脚本会换一张。

## License

MIT
